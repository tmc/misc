# Local testing environment variables

# Server configuration
PORT=8080

# Go module proxy testing
GOPROXY=direct
GOSUMDB=off

# For testing with restricted PATH
# Uncomment to test with minimal PATH (Go binary only)
# PATH=/usr/local/go/bin

# For testing module resolution locally  
# Point to local server instead of real go.tmc.dev
# GOPROXY=http://localhost:8080,direct