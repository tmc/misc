# Test that live content doesn't appear in static HTML generation

# Generate static HTML files
md2html -html static_output -html-ext html
exists static_output/index.html
exists static_output/test.html

# Verify live reload script is NOT included in static HTML
! exec grep -q 'EventSource' static_output/test.html

# Verify connection status div is NOT included in static HTML
! exec grep -q 'id="status"' static_output/test.html

# Verify no live reload related JavaScript
! exec grep -q 'onmessage.*reload' static_output/test.html

# Verify no SSE endpoints
! exec grep -q '/events' static_output/test.html

# Clean up
rm -rf static_output static_test_dir

-- static_test_dir/test.md --
# Static Test Document

This content should not include live reload functionality when generated as static HTML.

## Content

Regular markdown content that should be converted to static HTML without any live features.

-- static_test_dir/index.md --
# Static Index

This is the index file for static generation testing.

[Test Page](test.md)