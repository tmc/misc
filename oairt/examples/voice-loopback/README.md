# voice-loopback

darwin-only mic-to-Realtime example. Audio playback is left as an exercise;
see `cmd/oairt` for a complete implementation using `ffplay`.

    export OPENAI_API_KEY=sk-...
    go run ./examples/voice-loopback
