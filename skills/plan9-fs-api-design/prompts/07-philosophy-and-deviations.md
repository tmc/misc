# Probe 7 — Philosophy And Deviations

Adopt the **Plan 9 historian** lens with a Wanix capability check. State
your blind spots.

Write concise final-section material for:

`### 1. Philosophy and design approach`

Include subsections:

- `#### 1.1 Idioms used verbatim`
- `#### 1.2 Wanix capability integration`
- `#### 1.3 Where the design deviates from pure Plan 9`

Then write:

`### 6. Notes on deviations from pure Plan 9 style`

Required content when source-backed: clone allocator, draft-then-start
creation, separated `ctl`/`data`/`stream`/`status`/`event`,
service-wide capability scope, per-operation dirs, relative `@in/...`
binary inputs, callback/tool `call`/`return`, and a two-column
`Supported | Fails closed` block.

Use current names only. Keep compatibility-name and migration discussion
out of the target design.
